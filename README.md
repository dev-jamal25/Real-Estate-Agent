# Real-Estate-Agent
This project builds an AI real estate agent to predict and explain home prices from natural language queries. A two-stage LLM chain extracts features to a schema, runs an ML prediction, and interprets the result. The system is deployed via FastAPI and Docker with a web UI.
